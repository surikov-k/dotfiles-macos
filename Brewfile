# brew bundle dump --force

# Taps
tap "homebrew/bundle"
tap "homebrew/cask"
tap "homebrew/core"

# Brews (Packages)
brew "bat"
brew "httpie"
brew "exa"

# Casks (Applications)
cask "alfred"
cask "google-chrom"
cask "visual-studio-code"
cask "dropbox"
