- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.zshrc:
    ~/.gitconfig:
- create:
    - ~/work

- shell:
    - command: ./setup-homebrew.zsh
      stdout: true
      stderr: true
